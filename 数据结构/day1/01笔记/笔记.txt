算法解决问题方式：
1、分治（分布治理） 有明确目标 有明确的执行方式
   1、拆分
   2、解决小问题
   3、合并
2、最短路径   有明确目标 找到有效的执行方式
3、贪心（贪婪）没有明确目标 没有有效的执行方式

基于切片的C语言代码解决方案：
//创建一块内存空间 参数字节大小  返回值为void* 万能指针
//在堆区开辟空间
int* p = (int*)malloc(8 * 10);
//重新开辟一块堆内存空间
p = realloc(p, 160);
printf("%p\n", p);
//释放堆空间
free(p);